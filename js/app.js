;
var portfolio = {
	currentSlot : 0
};

/**
 * Contains the data loaded via IIFI (see prepareData.js).
 */
portfolio.data = [];

/**
 * Size of Each Slot.
 * This illustrates the number of images that will be loaded in one go.  
 */
portfolio.SlotSize = 8;

/**
 * Number of images we have.
 */
portfolio.TotalImages = 20;

